---
title: "Moradin Forge v0.2.0 Beta 3"
status: release-candidate
owner: moradin-forge
last_reviewed: 2026-07-31
source_refs:
  - ../../README.md
  - ../../Harness/moradin_payload/manifest.yaml
related_docs:
  - ../references/moradin_forge_agent_integration_contract_v1.md
  - ../references/tooling_readiness_install_execution_contract_v2.md
  - ../references/moradin_forge_tooling_suite_contract_v1.md
  - ../references/moradin_forge_upgrade_contract_v1.md
  - ../references/moradin_agent_efficiency_contract_v1.md
  - ../11_ops/air_gapped_tooling_suite.md
---

# Moradin Forge v0.2.0 Beta 3

## Summary

Beta.3 makes Forge a standalone universal-agent workstation baseline. Point an
agent at the README to get bounded discovery, a practical tooling plan,
separate approvals, transactional adoption and upgrades, and compact
context-preserving helpers without a private `.templates` dependency.

## Highlights

- Adds repeatable, approved multi-workspace onboarding and repository
  discovery without broad source or home-directory crawling.
- Adds the adaptive practical-full developer, security, supply-chain,
  container, release, and token-efficiency catalog.
- Adds one human-run Linux tooling suite with Install All and Customize menus,
  Practical and Extended profiles, apt/dnf/pacman support, amd64/arm64 assets,
  digest-bound sudo, root receipts, verification, and rollback.
- Seals the approved installer into a root-owned runner before sudo execution,
  uses a doubly hashed pinned uv bootstrap, and verifies new Podman installs in
  the target user's rootless context.
- Permits exact-digest-approved user-level installs and produces reviewable
  user-run Bash or PowerShell for privileged packages.
- Adds official-source resolution with a 24-hour invocation-time cache,
  execution receipts, rollback, and honest asset-only compatibility bundles.
- Adds complete target-specific Linux air-gap kits with sanitized requests,
  frozen locks, native package trust, sealed no-network apply, rollback closure,
  stale-kit consent, and deterministic lock rebuilds.
- Qualifies Ubuntu, Debian, Fedora, and Rocky air-gap kits on amd64/arm64 and
  frozen Arch kits on amd64. Arch arm64 fails closed instead of substituting
  the separate Arch Linux ARM port or an unofficial snapshot trust lane.
- Adds independently approved, marker-owned Codex, Claude, Gemini, Copilot, and
  Cursor integration with lowercase and near-miss warnings.
- Adds accessible qualitative, illustrative, and deterministic measured README
  figures, with release-dogfood byte evidence clearly separated from universal
  performance claims.
- Adds standalone context primer, state, repository brief, rerun advice,
  checkpoints, and sanitized local efficiency counters.
- Adds transactional V1/V2 sidecar upgrades with stale-plan protection,
  atomic cutover, byte-identical failure recovery, and one retained
  predecessor.
- Adds native Linux, macOS, and Windows contract validation while preserving
  the existing GitHub Free CI/security/dependency-submission split.

## Runtime Notes

- Forge core requires Python 3.12 or newer.
- The optional browser workbench requires Node.js 24 or newer.
- Tool update checks occur only when Forge runs and cached metadata is stale.
- There is no background updater.
- The normal release rollback target is `v0.2.0-beta.1`; transactional sidecar
  rollback and ownership lineage remain separate adoption contracts.

## Release Boundary

This is a prerelease candidate, not a production-readiness claim. Signing,
production readiness, release-candidate manifest, platform signing, and
production promotion remain deferred human-gated lanes.
