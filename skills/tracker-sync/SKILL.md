---
name: tracker-sync
description: Keep dev tracker snapshot and QA signals synchronized after docs/process changes.
---

# Tracker Sync

Run:

- `npm --prefix dev_tracker/ui run sync-docs`
- `npm --prefix dev_tracker/ui run test`
- `npm --prefix dev_tracker/ui run build`
