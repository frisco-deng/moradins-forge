# Architecture Brief

Canonical source: `docs/03_architecture/index.md`

The manager repo and downstream payload now use the same canonical naming model: snake_case docs paths, `Harness/` as control plane, and `docs/` as truth.
