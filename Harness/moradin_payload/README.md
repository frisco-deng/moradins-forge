# Moradin Payload

`Harness/moradin_payload/manifest.yaml` is the canonical bridge-first payload
contract for Moradin Harness deploy, sidecar, and seed-generation flows.

The legacy `.harness_template/` scaffold remains available for one
compatibility window, but operator-facing routes and docs should use Moradin
payload language.
