repos:
  - repo: local
    hooks:
      - id: push-gate
        name: push-gate
        entry: make push-gate
        language: system
        pass_filenames: false
        stages:
          - pre-push
